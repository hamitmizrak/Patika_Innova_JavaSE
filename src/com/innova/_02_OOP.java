package com.innova;

final public class _02_OOP { //extends
    //access modifier ==> public,protected, private ,   -
    // public    ==> borkör     = aynı paket(+) farklı  paket(+),  aynı class(+),  farklı class(+) subClass(+)
    // protected ==> araf       = aynı paket(+) farklı  paket(+ ? -), aynı class(+), farklı class(+) subClass(+) Dipnot: this. super.
    // private   ==> cimridir   = aynı paket(+) farklı  paket(-), aynı class(+), farklı class(-) subClass(-)
    // default   ==>            = aynı paket(+) farklı  paket( -), aynı class(+), farklı class(+) subClass(-)


    // oop nedir ?, Prensibleri nelerdir ? IPEA   ---------------------------
    // Inheritance (kalıtım)
    // Polymorphism(çok biçimlilik) ==> Tek yumurta ikizleri Java , C#
    // Encapsulation (kapsülleme) setter,getter
    // Abstraction (Soyutlama) = interface(arayüz) + abstract(soyut)
    // class
    // pojo
    // bean
    // managementBean
    // c.d.i
    // constructor
    // toString
    // getter setter
    // hashcode()
    // @Override ,Overloading
    // static
    // final
    // 1-) classlarda ==> extends yapamazsın
    // 2-)
    final public  void finalMethod(){ //Override edemezsin

    }
    // 3-) private
  /*  private final static String DEGISMEZ_DEGISKEN2="5415asd";
    public  final static String DEGISMEZ_DEGISKEN2="uu5415asd";*/

  public static void deneme(){
      System.out.println("MErhabalar");
  }
    public static void main(String[] args) {
        deneme();
    }

    // this,super
    // instanceof
    // lombok
    // inheritance
    // interface
    // abstract

    //template import
    //template sildiğimde automatic silmesi

    //Dummy codes
    //spagetti codes
    //clean codes
    //kiss
    //dry
    //yagni
    //ccp
    //monad
    //agile
    //scrum
    //mvc
    //jira
    //katmanlı mimari
    //bean
    //annotation
    //0553 485 9082


}
